# WebSocket Server URL (Optional)
# Set this to override the automatic relay server URL detection
# 
# By default, the application automatically detects the relay server:
# - localhost -> http://localhost:3000
# - avanderw.co.za -> https://high-society.avanderw.co.za
#
# Only set this if you need to override the automatic detection
# VITE_SOCKET_SERVER_URL=http://localhost:3000
